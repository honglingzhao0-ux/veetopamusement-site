# ============================================================
# VEETOP 独立站环境变量示例
# 使用方式：复制本文件为 `.env.local` 并按需填写（本地开发）。
# 部署到 Vercel 时，请在 Vercel 项目 Settings -> Environment
# Variables 中配置同名变量，无需 .env.local。
# ============================================================

# 图片 CDN 地址（将来指向 Cloudflare R2 的公开访问域名 / 自定义域名）。
# 例如 R2 公开 bucket 域名为 https://cdn.veetopamusement.com 时，
# 产品图 /images/products/xxx.jpg 会自动拼接为
# https://cdn.veetopamusement.com/images/products/xxx.jpg
#
# 不设置（留空 / 删除本行）时，所有图片自动回退到站内 /images 路径，
# 本地开发与 Vercel 部署都能直接显示，无需任何额外配置。
NEXT_PUBLIC_CDN_URL=https://cdn.veetopamusement.com

# 部署后在 Vercel 中请再添加（用于绝对 URL / sitemap / robots / OG）：
# NEXT_PUBLIC_SITE_URL=https://www.veetopamusement.com
# （不设置时，代码回退到 site.config.ts 中的 domain，见 site.config.ts REPLACE 标记）
